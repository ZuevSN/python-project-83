# Секретный ключ
# сгенерировать можно через
# import secrets
# secret_key = secrets.token_hex(16)
# print(secret_key)
SECRET_KEY=cfcd9647575378ef25fe4c526b05a7a95e6a111a4b60

# Строка подключения
# можно либо подключить базу лоакльно
# driver_provider://user_name:user_password@host:port/db_name
# либо на внешнем сервисе, в этом случае строка подключения будет предоставлена сервисом
# в случае render.com предоставляется строка как для внутреннего подключения, так и для подключения извне 
# driver_provider://user_name:user_password@host/db_name
DATABASE_URL=postgresql://user_123:Qwert123@localhost:5432/analyzer

# Режим отладки
# В случае True, сервер будет работать в режиме отладки
# Будут выводится подробныее сообщения об ошибке
# При изменении кода сервер будет автоматически перезапускаться
# В случае False, сервер будет работать в более безопасном, проиводительном режиме
# В продакшене не рекомендуется запускать сервер в режиме отладки
DEBUG=True

# В случае работы приложения на внешнем сервисе необходимо
# внести данные в Envirinment variables в виде ключ и значение